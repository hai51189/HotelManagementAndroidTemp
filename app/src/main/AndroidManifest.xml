<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.hotelmanagement.hotelmanagementproject">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".userlogin">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".userregister" />
        <activity android:name=".employeeregister" />
        <activity android:name=".guesthomepage" />
        <activity android:name=".receptionisthomepage" />
        <activity android:name=".resandentertainment" />
        <activity android:name=".manager"></activity>
    </application>

</manifest>